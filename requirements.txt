# Recomendado: ajustar versiones si tu código requiere otras específicas.
# Si no quieres rangos, reemplaza las líneas por versiones fijas (ej: Flask==2.3.2).

Flask>=2.2,<3
flask-cors>=3.0
mysql-connector-python>=8.0
gunicorn>=20.1.0

# Dependencias comunes para procesamiento y modelado (ajusta según uso)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.2

# Utilidades
python-dotenv>=1.0
requests>=2.30